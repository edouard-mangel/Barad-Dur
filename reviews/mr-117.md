# MR !117 — Docker job retry

No actionable correctness findings. Retrying all `script_failure` results can delay genuine failures, but that trade-off is explicitly documented and bounded to this job.
