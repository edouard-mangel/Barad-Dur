# MR !111 — Mirror only the release tag

No actionable findings. The release script creates annotated tags, so comparing the peeled local and remote tag targets is valid for the project's release path.
