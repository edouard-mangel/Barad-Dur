# MR !110 — Pre-1.0 version-bump semantics

No actionable findings. The pre-1.0 breaking-change mapping is covered at both sides of the 1.0 boundary, and `cargo update --workspace` avoids a general dependency re-resolution.
